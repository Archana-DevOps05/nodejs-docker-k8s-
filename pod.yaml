apiVersion: v1
kind: Pod
metadata:
  name: nodejs-pod  # Pod name is lowercase, which is correct
  labels:
    app: nodejs-app  # More common label naming convention
spec:
  containers:
    - name: node
      image: archanasingh05/nodejs-app  # Ensure this image name is correct and accessible
      ports:
        - containerPort: 5000

